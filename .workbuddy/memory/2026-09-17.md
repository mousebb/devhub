# 2026-09-17

- 从零搭建 DevHub v1（Windows 本地开发项目启动器，Electron + React + TS + Vite + Tailwind）
- 完成：项目/服务 CRUD、ProcessManager（spawn/日志/状态机/进程树停止）、配置层（%APPDATA%）、
  扫描器（package.json scripts 等自动识别）、端口占用检测、托盘、VS Code/终端/资源管理器快捷打开、
  设置页（主题/环境只读展示/工具检测/导入导出）、深浅色 UI
- 验证：tsc 通过、build 通过；`scripts/smoke-test.mjs` 验证 spawn 日志捕获 + taskkill 树杀 + 环境不变；
  在真实应用内用 e2e 演示项目验证 启动→实时日志→落盘→停止无残留，并截图 docs/screenshot-running.png
- 踩坑：ELECTRON_RUN_AS_NODE=1 导致 electron 退化成 Node → scripts/start.mjs 统一 unset；
  electron-vite v2 需显式 rollupOptions.input；PowerShell 工具本会话无输出，用 wmic 替代
- 补充：README 增加「怎么运行 / 常见问题 / 首次使用」章节
- 环境坑（后续会话注意）：bash 工具可能因 shim 丢失 PATH coreutils → 先
  `export PATH="/c/Users/mouse/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:.../bin:/c/WINDOWS/System32:/c/WINDOWS:$PATH"`；
  wmic 已被安全策略拉黑，查进程改用 PowerShell `Get-Process` + 写文件再 Read；
  agent 会话内直接 Start-Process 启动 electron 会 GPU 崩溃，需加 `--disable-gpu --no-sandbox`
  且要用 Start-Process（bash 后台启动会随工具调用结束被杀）
